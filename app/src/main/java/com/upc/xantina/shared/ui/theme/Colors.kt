package com.upc.xantina.shared.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales de Xantina basados en las imágenes
val XantinaPrimary = Color(0xFF492C1E) // Marrón café principal
val XantinaSecondary = Color(0xFFD2B48C) // Beige claro
val XantinaBackground = Color(0xFFFAF7F0) // Fondo off-white
val XantinaCardBackground = Color(0xFFFFFFFF) // Fondo blanco de las tarjetas
val XantinaTextPrimary = Color(0xFF000000) // Negro para texto principal
val XantinaTextSecondary = Color(0xFF666666) // Gris para texto secundario
val XantinaTextPlaceholder = Color(0xFFCCCCCC) // Gris claro para placeholders

// Colores para estados
val XantinaError = Color(0xFFD32F2F) // Rojo para errores
val XantinaSuccess = Color(0xFF388E3C) // Verde para éxito
val XantinaWarning = Color(0xFFF57C00) // Naranja para advertencias

// Colores para botones
val XantinaButtonPrimary = XantinaPrimary
val XantinaButtonSecondary = Color(0xFFE0E0E0) // Gris claro para botones secundarios
val XantinaButtonText = Color(0xFFFFFFFF) // Blanco para texto de botones primarios
val XantinaButtonTextSecondary = XantinaTextPrimary // Negro para texto de botones secundarios
